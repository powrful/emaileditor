export declare const ToggleButton: ({ id, selected, onChange, items, }: {
    id: string;
    selected: string;
    onChange: (id: string) => void;
    items: {
        id: string;
        label: string;
        icon: React.ReactNode;
    }[];
}) => import("react/jsx-runtime").JSX.Element;
